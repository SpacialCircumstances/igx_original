plugins {
    id 'application'
    id 'java'
}

sourceSets {
    main {
        java {
            srcDirs 'src'
        }
    }
}

dependencies {
    compile fileTree(dir: "${projectDir}/lib", includes: ['*.jar'])
}